# Appearance-Invariant-Gaussian-Grouping

## Dependencies
Environment setup and activation scripts for managing project dependencies. See `env_scripts/README.md` for usage.

## Stage-1
Contains the core code and experiments for the project: models, training and evaluation scripts, and dataset preprocessing used for the initial experiments. Use this folder to reproduce the baseline pipeline and results.

## Stage-2
Currently empty. Collaborators may copy folders or add files here to develop alternative approaches or extend the pipeline toward a unified workflow.

## Stage-3
Currently empty. Collaborators may copy folders or add files here to develop alternative approaches or extend the pipeline toward a unified workflow.